<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huawei.tool.dao.TaskDao">

    <insert id="insert">
        INSERT INTO t_profit_calculation_task (task_id, task_name, task_status, data_file, create_time)
        VALUES (#{taskId}, #{taskName}, #{status}, #{fileName}, NOW())
    </insert>

    <update id="updateStatus">
        UPDATE t_profit_calculation_task
        SET task_status = #{status}, fail_reason = #{failReason}, update_time = NOW()
        WHERE task_id = #{taskId}
    </update>

    <update id="markSuccessWithStatus">
        UPDATE t_profit_calculation_task
        SET task_status = #{status}, fail_reason = NULL, finish_time = NOW(), update_time = NOW()
        WHERE task_id = #{taskId}
    </update>

    <update id="markFailedWithStatus">
        UPDATE t_profit_calculation_task
        SET task_status = #{status}, fail_reason = #{reason}, update_time = NOW()
        WHERE task_id = #{taskId}
    </update>

    <delete id="delete">
        DELETE FROM t_profit_calculation_task WHERE task_id = #{taskId}
    </delete>

    <select id="findRow" resultType="com.huawei.tool.dao.model.TaskRow">
        SELECT * FROM t_profit_calculation_task WHERE task_id = #{taskId}
    </select>

    <select id="selectAllOrderByCreateTimeDesc" resultType="com.huawei.tool.dao.model.TaskRow">
        SELECT * FROM t_profit_calculation_task ORDER BY create_time DESC
    </select>
</mapper>
