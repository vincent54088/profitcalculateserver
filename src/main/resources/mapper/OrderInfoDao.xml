<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huawei.tool.dao.OrderInfoDao">

    <resultMap id="OrderRowResultMap" type="com.huawei.tool.dao.model.OrderRow">
        <result column="order_id" property="orderId"/>
        <result column="area" property="area"/>
        <result column="representative_office" property="representativeOffice"/>
        <result column="country" property="country"/>
        <result column="accounts" property="accounts"/>
        <result column="project" property="project"/>
        <result column="po_id" property="poId"/>
        <result column="product_domain" property="productDomain"/>
    </resultMap>

    <select id="selectByTaskId" resultMap="OrderRowResultMap">
        SELECT order_id, area, representative_office, country, accounts, project, po_id, product_domain
        FROM t_order_info WHERE task_id = #{taskId}
    </select>

    <select id="selectByTaskIdAndOrderId" resultMap="OrderRowResultMap">
        SELECT order_id, area, representative_office, country, accounts, project, po_id, product_domain
        FROM t_order_info WHERE task_id = #{taskId} AND order_id = #{orderId}
    </select>

    <update id="updateOrder">
        UPDATE t_order_info SET
            area = #{r.area},
            representative_office = #{r.representativeOffice},
            country = #{r.country},
            accounts = #{r.accounts},
            project = #{r.project},
            po_id = #{r.poId},
            product_domain = #{r.productDomain}
        WHERE task_id = #{taskId} AND order_id = #{r.orderId}
    </update>

    <insert id="batchInsert">
        INSERT INTO t_order_info (task_id, order_id, area, representative_office, country, accounts, project, po_id, product_domain)
        VALUES
        <foreach collection="rows" item="r" separator=",">
            (#{taskId}, #{r.orderId}, #{r.area}, #{r.representativeOffice}, #{r.country}, #{r.accounts}, #{r.project}, #{r.poId}, #{r.productDomain})
        </foreach>
    </insert>
</mapper>
